package com.expandtesting.Pages;


import org.openqa.selenium.*;
import org.openqa.selenium.support.FindBy;
import org.openqa.selenium.support.How;
import org.openqa.selenium.support.PageFactory;

public class PageRegister {
    /*WebDriver PageDriver;
    ScrollToElement scroller;

    @FindBy(how=How.XPATH,using="//input[@id='username']")
    WebElement username;

    @FindBy(how=How.XPATH,using="//input[@id='password']")
    WebElement password;

    @FindBy(how=How.XPATH,using="//input[@id='confirmPassword']")
    WebElement confirmPassword;

    @FindBy(how=How.XPATH,using="//button[@type='submit']")
    WebElement registerButton;

    public PageRegister(WebDriver driver) {
        PageFactory.initElements(driver,this);
        PageDriver = driver;
        scroller = new ScrollToElement();
    }

    public void register(String un,String pw) {
        scroller.ScrollToElement(PageDriver,username);
        username.sendKeys(un);
        scroller.ScrollToElement(PageDriver,password);
        password.sendKeys(pw);
        scroller.ScrollToElement(PageDriver,confirmPassword);
        confirmPassword.sendKeys(pw);
        scroller.ScrollToElement(PageDriver,registerButton);
        registerButton.click();
    }*/

}
